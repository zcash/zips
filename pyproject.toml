[project]
name = "zips-renderer"
version = "0.1.0"
description = "Installer for ZIP rendering dependencies"
requires-python = ">=3.11"
dependencies = [
    "docutils==0.21.2",
    "rst2html5==2.0.1",
]

[tool.hatch.build.targets.wheel]
packages = []
